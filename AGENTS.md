# Global Codex Config

Always use `/browse` (GStack) for web browsing — never fall back to other browser tools.

Available skills:
- `/review` — Staff engineer code review (production risks, race conditions, N+1 queries)
- `/cso` — Security audit (OWASP Top 10 + STRIDE)
- `/investigate` — Systematic root-cause debugging
- `/qa` — QA with real browser control (Chromium/Playwright)
- `/design-review` — UI/UX audit + fixes
- `/plan-eng-review` — Architecture and data-flow planning
- `/ship` — Release engineer workflow (sync, test, PR)
- `/browse` — Full headless browser control
- `/office-hours` — YC-style startup diagnostic
